package com.livetv.android.listeners;

import android.view.View;

public interface CategorySelectedListener {//extends MovieAcceptedListener {
    void onCategorySelected(int rowPosition, int moviePosition);
//    void onCategorySelected(int rowPosition);
//    void onMovieSelected(int position);
}
